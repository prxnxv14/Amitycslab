#include <stdio.h>

int main()    
{    
 int i,value=1,number;    
 printf("Enter a number: ");    
  scanf("%d",&number);    
    for(i=1;i<=number;i++){    
      value=value*i;    
  }    
  printf("Factorial of %d is: %d",number,value);    
return 0;  
}
