#include <stdio.h>

int main()
{
    int i,n,sum;
    printf("enter number");
    scanf("%d",&n);
    sum=0;
    i=1;
    while (i<=n)
    {
    sum=sum+i;
    i=i+1;
    }

    printf("sum=%d",sum);

    return 0;
}
