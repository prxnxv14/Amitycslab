#include <stdio.h>

int main()
{   
    int n;
    int sum=0;
    printf("enter number:",n);
    scanf("%d", &n);
    for(int i=1;i<=n;i++)
    {   
        if(i%2==1)
        {

            sum=sum+i;
        }
    }
    printf("sum of odd numbers upto n:%d\n", sum);
    return 0;
}
