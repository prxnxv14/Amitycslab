#include <stdio.h>

int main() {
    int n, i,c;
    printf("Enter an integer: ");
    scanf("%d", &n);
    printf("Enter till where the multiplication table should be done:");
    scanf("%d", &c);
    for (i = 1; i <=c; ++i) {
        printf("%d * %d = %d \n", n, i, n * i);
    }
    return 0;
}
